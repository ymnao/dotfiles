# eval fixture: README with typos

This is a fixture file used by dev/02 (no-arg / HANDOFF) and dev/03 (freeform)
evals. It contains deliberate typos that the eval expects the skill to fix.

- `teh` should be `the` (used by dev/03)
- `fooo` should be `foo` (used by dev/02)

Do not edit these typos; the evals depend on their presence.

## Sample section

If teh weather is nice, take a walk. The variable fooo controls the loop.
