{
  "name": "dotfiles",
  "private": true,
  "description": "Dev tooling for the dotfiles repo (secretlint, etc.)",
  "scripts": {
    "lint": "git ls-files -z | xargs -0 secretlint --"
  },
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "^13.0.0",
    "secretlint": "^13.0.0"
  }
}
